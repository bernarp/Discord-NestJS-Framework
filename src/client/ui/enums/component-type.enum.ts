/**
 * Enum for UI component types used in ComponentContainerBuilder.
 */
export enum ComponentType {
    TEXT = 'text',
    SEPARATOR = 'separator',
    ACTION_ROW = 'actionRow',
    SELECT_MENU = 'selectMenu',
    FILE = 'file'
}
